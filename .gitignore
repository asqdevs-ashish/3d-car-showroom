# Dependencies
/node_modules

# Next.js build output
/.next
/out
/build

# Runtime + env
*.log
.env*
!.env.example

# Typescript
*.tsbuildinfo
next-env.d.ts

# OS noise
.DS_Store
Thumbs.db

# ------------------------------------------------------------------
# Original (unoptimised) car models
# ------------------------------------------------------------------
# Kept locally so an optimisation pass can be re-run or rolled back.
# They are 243 MB and are NOT needed to build or run the app — the
# optimised versions in /public/models are what ship.
/models-original